#!/usr/bin/env bash

# this file is used to run sage from builddir.
# it will *not* be installed.

export SAGE_ETC=@abs_top_builddir@
export LD_LIBRARY_PATH=@libdir@
export PYTHONPATH=@abs_top_builddir@/../
export PATH=@abs_top_builddir@:$PATH
export PATH=@abs_top_srcdir@:$PATH
export SAGE_SRC=@abs_top_srcdir@/../


# TODO: some heuristics to detect sage-the-distribution in ../.. (?)

$0.real
# vim:et:sw=4:
